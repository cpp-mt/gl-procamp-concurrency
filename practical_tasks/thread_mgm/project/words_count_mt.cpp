#include <iostream>
#include <iterator>
#include <string>
#include <fstream>
#include <algorithm>
#include <vector>
#include <unordered_map>

#include "words_count_common.h"

// take list of file names on command line,
// print <limit> most common words within;
std::vector<WordCountMapType::value_type>
word_count_mt(const std::vector<std::string>& filenames, size_t limit)
{
   // TODO
   std::vector<WordCountMapType::value_type> res;
   return res;
}
